{
  "name": "object-array-filter",
  "version": "1.0.1",
  "description": "Filters an array of objects by matching a field's value in each object to a search term. Returns filtered array",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/zerico007/object-array-filter"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "dev": "nodemon --watch './**/*.ts' --exec ts-node src/index.ts",
    "build": "tsc",
    "prepare": "npm run build"
  },
  "keywords": [
    "object",
    "JavaScript",
    "javascript",
    "filter",
    "array"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.7",
    "ts-node": "^10.0.0",
    "typescript": "^4.3.2"
  }
}
